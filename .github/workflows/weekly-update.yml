name: Weekly data update

on:
  schedule:
    - cron: "0 13 * * 2"
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: weekly-update
  cancel-in-progress: false

jobs:
  update:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v7

      - name: Set up Python
        uses: actions/setup-python@v7
        with:
          python-version: "3.11"
          cache: pip

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Fetch latest results
        run: |
          set +e
          python update_data.py
          status=$?
          set -e
          if [ "$status" -gt 1 ]; then
            echo "update_data.py failed with exit code $status"
            exit "$status"
          fi
          if [ "$status" -eq 0 ]; then
            echo "New matches were added"
          else
            echo "No new matches this week"
          fi

      - name: Retrain and rebuild projections
        run: python main.py

      - name: Commit refreshed files
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          if git diff --cached --quiet; then
            echo "Nothing changed, skipping commit"
            exit 0
          fi
          git commit -m "Weekly update: refresh results and projections"
          for attempt in 1 2 3; do
            if git push; then
              echo "Pushed on attempt $attempt"
              exit 0
            fi
            echo "Push rejected on attempt $attempt, rebasing on latest main"
            git pull --rebase origin "${GITHUB_REF_NAME}" || true
            sleep 5
          done
          echo "Could not push after 3 attempts"
          exit 1
